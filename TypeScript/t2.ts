const subject:readonly string[] = ["Maths","Science"]

subject.push("67")

console.log(subject)

const person : {id:number, name:string, age?:number} = {
    id: 101,
    name: "Nisha",
}

console.log(person.id)